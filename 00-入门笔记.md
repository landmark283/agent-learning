# Agent 编程入门笔记：用 LLM 做软件开发

> 面向对象：刚分流到 AI 专业、对训练底层逻辑暂时不感兴趣、但想用 LLM 做开发的人。
> 核心立场：**Agent 不是"更聪明的模型"，而是"给笨模型配的自动驾驶系统"。**
> 你需要补的不是训练知识，而是系统思维 + 工程手段 + 一点编程功底。

---

## 0. 一句话总结

LLM 本质上是一个概率文本生成器，会自信地"编造"（幻觉）。工业界对付它的办法不是让模型变可靠，而是**在模型外面套一层确定性的"壳"：让它不断在真实环境里行动、被检验、错了就改**。这层壳就是 Agent 的架构。

---

## 1. 心智模型：模型是"演员"，Agent 是"剧组"

| 角色 | 承担者 | 职责 |
|---|---|---|
| 演员（只会说台词） | LLM | 生成"像样的下一段文字"：思考、计划、写代码片段 |
| 导演（编排流程） | 确定性代码 | 决定何时调用模型、何时执行、何时结束 |
| 手脚 | 工具层 | 读写文件、跑命令、检索、git |
| 裁判 | 真实环境 | 编译器、测试、构建结果——它们不说谎 |

**关键认知**：模型没有记忆、不会执行代码、不验证自己说的话。所谓幻觉，是因为它没有"知道/不知道"的开关，只有"像/不像"。所以一切可靠性都来自壳，而不是模型本身。

---

## 2. 核心架构：ReAct 循环（想-做-看-改）

几乎所有编程 Agent 都是同一个骨架：**ReAct（Reason + Act）循环**。

```
用户任务 ──► 模型"想"(规划下一步)
                 │
                 ▼
          调用工具(读文件/改代码/跑测试/查文档)
                 │
                 ▼
        观察结果(报错信息/测试输出/文件内容) ──► 模型再"想"
                 │                                │
                 └──────── 直到任务完成 ◄─────────┘
```

每轮迭代三件事：

1. **想**：模型生成一次"思考 + 一个工具调用"（输出必须符合工具 schema）；
2. **做**：由确定性代码执行这个调用，而不是模型自己执行；
3. **看**：把真实结果（尤其是报错）原样喂回给模型。

模型的"记忆" = 对话里不断累积的观察结果。上下文窗口就是它的工作台。

### 典型工具层清单

- **文件操作**：读、写、查找（带精确上下文匹配，见 §3.3）
- **执行环境**：shell 命令、编译、跑测试、装依赖
- **检索**：grep、代码搜索、向量检索仓库文档
- **版本控制**：git diff、commit
- **外域**：浏览器、外部 API

---

## 3. 防幻觉的四大类防线

> 哲学：**幻觉无法根除，但可以让它"便宜且可被发现"。**

### 3.1 接地（Grounding）：只准说"亲眼看过"的东西

- **RAG 式仓库索引**：把代码库向量化/索引，动手前先检索相关文件，把真实内容放进上下文——改的是"读过的那几行"，不是"凭印象猜的类名"。
- **代码图谱 / LSP**：借用编译器的符号表（"跳转到定义"），拿到精确的函数签名和类型。
- **Repo Map**（Aider 首创）：给模型一份"仓库地图"，先看地图再决定读哪些文件，避免巨仓瞎翻。

原则：**幻觉的根源是"没依据地编"，那就强迫它每一步都有依据。**

### 3.2 验证回路（Verification Loop）：环境当裁判，模型当选手

这是编程 Agent 比通用 Chatbot 可靠得多的根本原因：

- 模型改完代码 → Agent **自动跑 lint、类型检查、单元测试、构建**
- 报错/失败 → 原样喂回 → 模型修复 → 再跑
- 直到测试全绿或迭代次数超限

**编译器、测试用例、构建结果是确定性事实。** 模型每次"自信的胡扯"都在测试这关被打脸并被纠正。这等于把幻觉检测外包给了机器本身。

### 3.3 结构化约束：不给自由发挥的空间

- **Function Calling**：模型只能输出符合 JSON Schema 的工具调用，非法输出直接拒绝重试——从机制上堵死自由文本胡写。
- **精确编辑格式**：改代码用 SEARCH/REPLACE（先搜到唯一一段原文，再替换），原文对不上就失败，而不是整文件重写——防止"顺手把旁边 50 行也改了"。
- **小步提交**：一次只改一个函数，跑一次测试，而不是一口气写 500 行。步骤越小，验证点越密，出错定位越容易。

### 3.4 人机协同与降级策略

- **权限门控**：危险操作（git push、删文件、装依赖）先问人类。Claude Code、Codex CLI 都是"自主循环 + 关键节点人类确认"。
- **沙箱隔离**：云端 Agent 在独立容器里执行，炸了也不影响本地环境。
- **审查 Agent**：写代码的 Agent 和审查 PR 的 Agent 分开，用第二双"眼睛"交叉检查。

---

## 4. 工业形态光谱（2025）

| 形态 | 代表 | 特点 |
|---|---|---|
| 补全 | GitHub Copilot 传统模式 | 模型只是打字机，无循环 |
| IDE 内 Agent | Cursor、Windsurf | 编辑器里的循环，能改多文件、跑命令、看报错 |
| 终端 Agent | Claude Code、Codex CLI、Aider | 命令行里的自主循环，权限门控，适合大任务 |
| 云端 Agent | OpenAI Codex（云版）、Devin、Google Jules | 独立环境跑长任务，给个 issue 它做完提 PR |
| 开源研究参考 | OpenHands、Aider | 架构透明，绝佳学习对象 |

**效果基准**：SWE-bench —— 从真实 GitHub issue 取任务，看 Agent 能否修好并让测试通过。看各家分数演进，能直观感受"验证回路"对效果的决定性作用。

---

## 5. 学习路径（给你的行动清单）

1. **亲手搭一个最小 ReAct 循环**（最值钱的一步）：用任意支持 Function Calling 的 API（OpenAI / Claude / DeepSeek），写一个 100 行左右的小 Agent，给模型"读文件""跑命令"两个工具，让它修复一个故意写错的小项目。第一次它大概率给错答案，但把报错喂回去它就对了——这就是幻觉被工程驯服的现场。
2. **读开源实现**：Aider 和 OpenHands 代码量可控，看它们怎么组织 system prompt、怎么设计工具 schema、怎么做验证。
3. **补三块基础**：提示词工程、函数调用/工具调用机制、RAG 基本原理。
4. **学会评估（Evals）**：理解 SWE-bench 这类基准——没有评估，就不知道改动是变好还是变坏。
5. **心态**：专业里的 ML/DL 课程会给你底层直觉（概率、上下文窗口、注意力），现在不感兴趣可以放着，用到时再回来拿——它们是理解"模型为什么这么错"的钥匙。

---

## 6. 参考链接

- [OpenHands vs Aider（沙箱 Agent vs 结对编程助手）](https://wetheflywheel.com/en/comparisons/openhands-vs-aider/)
- [Why Claude Needs a Real Environment to Validate Cloud-Native Code](https://www.signadot.com/blog/claude-cloud-native-validation/)
- [Why Capable Agents Still Fail（强模型 ≠ 可靠执行）](https://walkinglabs.github.io/learn-harness-engineering/en/lectures/lecture-01-why-capable-agents-still-fail/)
- [OpenHands 开源仓库](https://github.com/OpenHands/OpenHands)
- [The Agentic Coding Workflow（AI Agent 指南书）](https://github.com/ai-infra-curriculum/ai-agent-guidebook/blob/main/best-practices/agentic-workflow.md)

---

## 7. 本仓库结构约定

```
agent-learning/
├── 00-入门笔记.md      ← 本文档
├── try/                ← 个人练习：自己写简易 Agent（纳入 git 管理）
└── source/             ← 源码阅读：克隆开源项目（不纳入 git，见 .gitignore）
```

- `try/` 里写的代码会提交到本仓库。
- `source/` 里 `git clone` 进来的项目自带 git 管理，与父仓库无关（嵌套 git 的处理方式见 README）。
