# 学习路径：从 Agent 常识到 DSH 插件开发（暑假密集版）

> 已确认的个人设定（2025 暑假）：
> - **节奏**：暑假密集突击，每天 2~3 小时，约 5 周
> - **基础**：JS/TS 已会 → 跳过 TypeScript 速成，直接进主题
> - **理论**：概念够用 + 动手为主 → 不啃论文，边做边补
> - **终点**：自用魔改，把自己的 profile 玩明白 → 不含开源协作流程
>
> 总目标：**弄懂常见 agent 组织形式与技巧 → 掌握 Cordis 架构 → 能开发/魔改 DSH 插件。**

---

## 0. 目标拆解（四级能力）

| 级别 | 能力 | 验收标准 |
|---|---|---|
| L1 | 常见 agent 组织形式 | 能说出单循环/分层/多 agent/workflow 的区别和适用场景 |
| L2 | 常见技巧 | 能解释上下文压缩、验证回路、沙箱、权限门控、结构化输出为什么能防幻觉 |
| L3 | Cordis 架构 | 能讲清 Context / Service / Fiber / 事件 / 配置驱动加载，并写出独立插件 |
| L4 | DSH 插件开发 | 能写出一个真实可用的 dsh 插件并装进自己的 profile |

**贯穿纪律（每天）**：
- 每次学习都 git commit，README 里的学习日志表随手填一行；
- 每读完一个包/项目写"一页纸"笔记（它解决什么问题 / 暴露什么 service / 依赖谁 / 验证点在哪）；
- 卡住超过 2 小时：去读对应包的 README 或换任务，不硬啃。

---

## 1. 第一件事：建立 DSH 生态地图（阶段 0，2~3 天）

DSH 本质 = **Cordis 插件框架 + 180+ 个实现 agent 能力的插件包**。先看地图再进门。

- 通读 `00-入门笔记.md`（心智模型 / ReAct / 防幻觉四防线）；
- 对照 `00-参考-DSH包清单.md`（已自动生成，含每个包的一句话描述），把包按类别重排，形成**你自己的**地图：
  - 引导与 CLI：`dsh`、`dsh-app-boot`、`dsh-cordis-host-runner`、`dsh-cmdline`
  - agent 循环：`dsh-agent`、`dsh-agent-loop`、`dsh-agent-presets`、`dsh-system-prompt`
  - 工具：`dsh-tools`、`dsh-tool-*`（ask-user / bash / fs / pwsh / todo / web / workflow / str-replace-editor …）
  - 上下文管理：`dsh-session`、`dsh-compaction-basic`、`dsh-token-meter`、`dsh-time-context`、`dsh-spill-*`
  - 沙箱与安全：`dsh-sandbox-*`、`dsh-fs-observation-policy`、`dsh-pwsh-sandbox`、`dsh-fs-sandbox`
  - 高层编排：`dsh-goal*`、`dsh-subagent*`、`dsh-workflow*`
  - 技能：`dsh-skill*`
  - 存储/设置：`dsh-settings*`、`dsh-storage*`、`dsh-credentials*`
  - 客户端 UI：`dsh-client-*`、`dsh-web-*`
- 环境自检：确认 `pnpm` 可用；在 DSH 源码仓库根目录 `pnpm run build` 后能 `pnpm dsh --profile web` 从 TS 源码跑起来（见 dsh 包的 README.zh.md "开发" 一节）。

**产出**：一份你自己的 DSH 生态地图（分类 + 一句话职责）。

---

## 2. 阶段 1：Agent 通用理论与最小 ReAct（第 1 周）

**概念（概念级理解即可，不读论文）**，每个概念配一个"在 DSH 里对应谁"的对照：

| 概念 | 一句话 | DSH 对应 |
|---|---|---|
| ReAct 循环 | 想-做-看-改 的迭代 | `dsh-agent-loop` |
| Function Calling | 模型只能输出符合 schema 的工具调用 | `dsh-tools` / `dsh-tool-*` |
| 验证回路 | 用测试/编译器当裁判 | 沙箱包 + 工具链 |
| 上下文管理 | 压缩/投影/剪枝，控制窗口 | `dsh-compaction-*`、`dsh-session-projection` |
| 沙箱与权限 | 隔离执行 + 危险操作门控 | `dsh-sandbox-*`、`dsh-user-approval` |
| 多 agent 协作 | 子代理/审查代理 | `dsh-subagent*`、`dsh-tool-subagent` |
| workflow | 确定性编排 vs agent 自由发挥 | `dsh-workflow*` |
| RAG | 先检索后作答，接地 | `dsh-tool-fs-search`、`dsh-skill-filesystem` |

**动手（重头戏，D4~D7）**：`try/01-mini-react` —— 用 DeepSeek API 的 Function Calling 写最小 ReAct 循环：
- 给模型两个工具：`read_file`、`run_command`；
- 准备一个故意写坏的小项目（函数名拼错、测试不通过）；
- Agent 自己读文件 → 跑测试 → 看报错 → 修复 → 直到测试全绿。

**产出**：`try/01-mini-react` 跑通 + 概念笔记（含上面的对照表）。

**资料**：网络现成路线（任选 1 个跟读）：
- [Hamed233/AI-Agents-Automation-Roadmap](https://github.com/Hamed233/AI-Agents-Automation-Roadmap)
- [Navidreza80/AI_Agents](https://github.com/Navidreza80/AI_Agents)
- [v9ai/ai-engineer-roadmap](https://github.com/v9ai/ai-engineer-roadmap)（agent harnesses 章节）
- Manning《Build an AI Agent from Scratch》[ReAct 章节](https://livebook.manning.com/book/build-an-ai-agent-from-scratch/chapter-4)
- 清华 2025《AI Agent 开发：零基础构建复合智能体》可当中文参考书

---

## 3. 阶段 2：读通用开源 Agent（第 2 周）

目的：看"别人怎么搭循环、怎么防幻觉"，为读 DSH 做准备。

1. **Aider**（2~3 天）：Repo Map、SEARCH/REPLACE 编辑格式、git 自动提交循环。`git clone --depth 1` 到 `source/aider`。
2. **OpenHands**（3~4 天）：事件驱动循环、sandbox、工具注册。`git clone --depth 1` 到 `source/openhands`。

**每读完一个写一页纸**（存进 `source/` 或 `notes/`）：它的循环长什么样 / 验证点在哪 / 用了哪些防幻觉技巧 / 如果你重写会改什么。

---

## 4. 阶段 3：Cordis 框架（第 3 周）

核心概念：**Context（依赖容器）→ Service（可注入服务）→ `inject`（声明依赖）→ Fiber（插件生命周期，dispose 即清理）→ 事件（`ctx.on/emit`）→ schemastery（配置 schema）→ 配置驱动加载（loader + YAML）**。

- D1~2：读本地 `@deepseek-ai/cordis`（`node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/cordis`），跑通 README 里的 Quick Start（counter/greeter）；
- D3~4：读 cordis 包内 `docs/` 教程（build-a-plugin / plugin-lifecycle / loader-config）+ [Koishi 中文文档的可逆插件系统](https://koishi.chat/zh-CN/cookbook/design/disposable)（Cordis 生态，讲"为什么插件可逆"）；
- D5~7：动手 `try/02-cordis-playground`：写一个带配置项的插件（用 schemastery 定义配置）、监听事件、注入 Service。

**产出**：`try/02-cordis-playground` 跑通 + 一页纸 Cordis 概念图（Context/Service/Fiber/事件/配置的关系）。

---

## 5. 阶段 4：DSH 源码精读（第 4 周，运行轨迹法）

按"一次会话从启动到回答"的路径读，只看骨架、不逐行：

| 天 | 读什么 | 看什么 |
|---|---|---|
| D1 | `dsh` CLI + `dsh-app-boot` + `dsh-cordis-host-runner` | profile 如何引导、插件如何被加载 |
| D2 | `dsh-agent-loop` | **核心 ReAct 循环在哪实现**：思考→工具→观察→再思考 |
| D3 | `dsh-tools` + 2~3 个 `dsh-tool-*` | 工具如何注册成 Cordis 插件、schema 怎么定义 |
| D4 | `dsh-session` + `dsh-compaction-basic` | 上下文如何管理、何时压缩 |
| D5 | `dsh-sandbox-*` + `dsh-fs-observation-policy` + `dsh-pwsh-sandbox` | 安全壳：命令怎么被隔离、文件访问怎么被管 |
| D6 | `dsh-goal*` + `dsh-subagent*` + `dsh-workflow*` | 高层编排：目标循环、子代理、工作流 |
| D7 | 汇总 | 画一张"DSH 运行时全景图" |

**产出**：`notes/` 下每个包一页纸笔记 + 一张全景图。

参考：[NanmiCoder/dsh-agent-teams 的 DSH 插件开发文档](https://github.com/NanmiCoder/dsh-agent-teams/blob/main/docs/developing-dsh-plugins.md)（社区指南）；DSH 上游仓库 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)（monorepo，本地 node_modules 是它 build 后的产物）。

---

## 6. 阶段 5：毕业项目——自研插件（第 5 周）

**选题（按推荐度）**：
1. **新工具插件** `dsh-tool-xxx`（推荐，最容易见效）：给模型加一个自定义工具，如"读指定目录的 README 并总结"；
2. **压缩策略魔改**：基于 `dsh-compaction-basic` 改一种自己的摘要/剪枝策略；
3. **skill 编写**：用 `dsh-skill*` 机制写一个你自己的技能包；
4. 会话持久化：给 `dsh-session-persistence-*` 换/加一种存储。

**流程**：设计（提供什么 service/工具？挂在哪？配置是什么）→ 实现 → 本地验证（`pnpm build` 后 `pnpm dsh` 跑 TS 入口，或 `dsh plugin` 装进 profile）→ 打磨 → 写总结 README。

**产出**：一个真实可用的插件 + 设计/踩坑记录（写进 `notes/`）。

---

## 7. 全程参考资源汇总

| 类别 | 资源 |
|---|---|
| 网络学习路径 | [AI-Agents-Automation-Roadmap](https://github.com/Hamed233/AI-Agents-Automation-Roadmap)、[AI_Agents](https://github.com/Navidreza80/AI_Agents)、[ai-engineer-roadmap](https://github.com/v9ai/ai-engineer-roadmap) |
| 书籍 | Manning《Build an AI Agent from Scratch》、清华《AI Agent 开发：零基础构建复合智能体》 |
| Cordis | 本地 `@deepseek-ai/cordis` 源码 + 包内 docs/ + [Koishi 文档](https://koishi.chat/zh-CN/cookbook/design/disposable) |
| DSH | 本地 node_modules 全部源码 + [deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) + [dsh-agent-teams 插件开发指南](https://github.com/NanmiCoder/dsh-agent-teams/blob/main/docs/developing-dsh-plugins.md) |
| 本仓库 | `00-入门笔记.md`、`00-参考-DSH包清单.md`、`notes/`（一页纸笔记）、`try/`（练习）、`source/`（克隆的源码） |
