# 学习路径（修订版）：实践先行，从 200 行到 196 包

> 已确认的个人设定：暑假密集（每天 2~3h）· JS/TS 已会 · 概念够用+动手为主 · 终点=自用魔改 DSH 插件。

## 修订说明（回应上次的两点不满）

1. **实践先行**：第 1 天就写最小 agent（零框架，几十行），理论永远跟在动手后面；每个概念= 一次对玩具 agent 的动手升级，不存在"先听三天课再动手"。
2. **由简到繁的阶梯**：不再一上来就碰生产级（DSH 是 196 个包，不适合当第一本教材）。学习对象按代码复杂度排成阶梯：**200 行 → 自己长大的 toy → smolagents → Claude Code（只看行为）→ Aider/OpenHands → Cordis → DSH**。
3. Claude Code 的定位：它**闭源**，不能读代码，但官方文档和社区逆向的系统提示词极其完整——它是学"生产级系统设计决策"的最佳教材，放在读 DSH 之前当心理准备，而不是代码阅读对象。
4. 方法论升级：**预测-对照法**——读任何真实系统前，先写下"如果我来写会怎么设计"，读的时候对照，差距就是收获。这比闷头读代码效率高得多。

---

## 学习路径的两条线：实践 + 理论

学习内容分两条线，互相索引：

- **实践线（本文件）**：实际学习顺序，按周推进；
- **理论线（`02-Agent架构理论清单.md`）**：需要掌握的 agent 架构与设计思路，按主题分组、编号。

**约定**：每个实践环节标注它落地的理论条目（编号见 02 清单）；以后生成任何单个环节的学习文档，都必须带"对应架构"标注。

### 实践 ↔ 理论映射表

| 实践环节 | 对应理论条目（见 02 清单） |
|---|---|
| W1-D1 · T0 最小 agent | #1 ReAct、#6 Function Calling |
| W1-D2 · T1 teenycode | #1 ReAct 完整组织、#22 线性循环 |
| W1-D3 · 自己 agent 加工具+自愈（已完成） | #6 工具调用、#1 观察-反馈 |
| W1-D4 · fork 补全①：run_command + 验证回路 | #16 验证回路（防幻觉第一课） |
| W1-D5 · fork 补全②：上下文压缩 + 轮数上限 | #10 上下文工程 |
| W1-D6 · fork 补全③：权限门控 | #18 权限门控/HITL、#20 降级 |
| W1-D7 · fork 补全④：记忆文件 + grep 检索 | #11 记忆分层、#8 RAG 最小形态 |
| W2-D1~3 · T3 smolagents | #3 CodeAgent |
| W2-D4~7 · T4 Claude Code | #2 Plan-and-Execute、#11 CLAUDE.md、#18 权限模型、#14 评审 |
| W3-D1~4 · T5a Aider | #9 ACI、#16 差分编辑验证 |
| W3-D5~7 · T5b OpenHands | #22 事件驱动、#17 沙箱隔离 |
| W4 · T6 Cordis | #21 插件化框架 |
| W5 · T7 DSH 精读 | #10/#12/#13/#6/#7/#17 生产级落地 |
| W6 · T8 毕业项目 | 综合运用 |

## 教学文档生成约定

本仓库的教学内容生成遵守三条规范（已固化为 skill：`.dsh/skills/lesson-author/SKILL.md`，
以后任何 agent 在本仓库教学都会自动遵守）：

1. **综合网络教程**：每个主题综合 2~3 个公开教程/官方文档，标注参考链接，不凭空发明；
2. **结构简洁、逻辑清晰**：统一骨架（目标→概念→动手→验收→参考），一份文档一个主题；
3. **具体举例**：抽象概念必配例子，优先用本仓库/DSH 实例；顺序固定为"为什么→是什么→怎么用"。

---

## 学习对象阶梯（总览）

| 阶梯 | 对象 | 体量 | 学什么 | 怎么学 |
|---|---|---|---|---|
| T0 | 零框架最小 agent（自己写） | 50~100 行 | 循环骨架：想-做-看-改 | 动手（第 1 天） |
| T1 | [teenycode](https://github.com/yangshun/teenycode)（200 LOC 编码 agent） | 200 行 | 最小编码 agent 的完整组织 | 读代码 + 对照 |
| T2 | 自己的 toy agent 升级 ×6 | 500~1000 行 | 工具/验证回路/压缩/权限/记忆/检索 | 动手，每个概念=一次升级 |
| T3 | [smolagents](https://huggingface.co/blog/smolagents)（HuggingFace） | 核心千行级 | CodeAgent：用代码当动作而非 JSON | 读 + 解剖笔记 |
| T4 | Claude Code（闭源，行为层） | — | 生产级系统设计决策 | 官方文档 + 逆向提示词 |
| T5 | Aider → OpenHands | 万行级 | 更大系统的架构组织 | 读，逐步变大 |
| T6 | Cordis | 框架 | 插件化机制（Context/Service/Fiber） | 读本地源码 + 玩具插件 |
| T7 | DSH | 196 包 | 所有技巧的生产级落地 | 运行轨迹精读 |
| T8 | 自研插件 | — | 输出 | 毕业项目 |

**阶梯原则**：每一级只引入一个"新复杂度"；读真实代码前先预测，读后只记"差距点"。

---

## 第 1 周：从零写出第一个 agent，并让它长大

### D1 · T0 最小 agent（零框架，先跑起来）

- 单文件 `try/01-agent-from-zero/agent.js`，不许用任何框架：
  - 调 DeepSeek API（`chat/completions` + `tools` 参数）；
  - 定义 **1 个工具**（如 `get_current_time` 或 `read_file`）；
  - `while` 循环：模型请求工具 → 执行 → 结果喂回去 → 直到模型给出最终回答。
- 参考：[阿里云《不使用框架，手写一个最小 Agent》](https://developer.aliyun.com/article/1757224)、Manning《Build an AI Agent from Scratch》[ReAct 章节](https://livebook.manning.com/book/build-an-ai-agent-from-scratch/chapter-4)。
- **验收**：能让模型通过"调用工具"回答一个它凭记忆答不了的问题。

### D2 · T1 读 teenycode（200 行编码 agent）

- `git clone --depth 1 https://github.com/yangshun/teenycode.git source/teenycode`。
- 阅读任务（200 行，半天足够）：系统提示词怎么写 / 工具怎么定义 / 循环怎么终止 / 出错怎么处理 / 与 DSH 的 `dsh-agent-loop` 比，骨架其实一模一样。
- **对照作业**：画它的循环图，列出"我没想到的 3 个细节"（写进 `notes/teenycode.md`）。

### D3~D7 · 转向：teenycode fork 补全计划

> **决策（学习者自选）**：toy 的使命已完成（最小循环、多轮对话、工具、自愈都亲手写过），
> 从 D4 起直接在 **fork 的 teenycode**（`source/teenycode`，已改造支持 DeepSeek）上补全缺口——
> 从"从零写"转为"读代码 + 改代码"的真实开发模式，也是 W5 读 DSH 的预演。

补全清单（基于源码的缺口分析，详见 `try/03-teenycode-改造/README.md`）：

| 天 | 补全项 | 背后概念 |
|---|---|---|
| D4 | ① `run_command` 工具 + ② 验证回路（改完自动跑测试，报错回喂） | #16 防幻觉第一课 |
| D5 | ③ 内层循环轮数上限（修缺陷）+ ④ 上下文压缩 | #10 上下文工程 |
| D6 | ⑤ 权限门控：危险命令先确认 | #18 安全 |
| D7 | ⑥ 启动自动读 AGENTS.md（记忆）+ ⑦ grep 检索 | #11、#8 |

**产出**：fork 仓库（`source/teenycode`）里 4 个补全 commit + `notes/teenycode-改造日志.md`。

---

## 第 2 周：读一个真实框架，研究一个生产级系统

### D1~D3 · T3 smolagents（HuggingFace）

> **⚠️ 已搁置（2026-08-25）**：学习者判断"代码派 vs JSON 派"是伪对立——现实系统
> （DSH/Claude Code/自己的 fork）都是工具优先 + 代码/命令兜底的混合形态。
> 源码已抓取至 `source/smolagents`，引导文档在 `try/04-smolagents/README.md`（含行号精读指引），
> 日后想精读"解释器隔离/authorized_imports"再回来。建议跳到 D4。

- 读 [Introducing smolagents](https://huggingface.co/blog/smolagents) + 它的核心代码（`git clone --depth 1 https://github.com/huggingface/smolagents.git source/smolagents`）。
- 核心思想：**CodeAgent——让 agent 直接写代码当动作，而不是输出 JSON 工具调用**。为什么？动作表达力更强、token 更省、天然可调试。这是与"JSON 工具调用派"的关键分叉，也是业界两大流派。
- 参考 [framework-analysis 的 tier 体系](https://github.com/larsderidder/framework-analysis)（它把框架按复杂度分了层），顺手建立"框架谱系"认知。
- **产出**：`notes/smolagents.md`（CodeAgent 怎么实现：代码生成 → 执行 → 报错回喂；与我的 JSON 版 toy 对比）。

### D4~D7 · T4 Claude Code 行为层研究（不开源代码）

Claude Code 闭源，但它是"生产级单 agent 系统"的最佳解剖样本：

- 官方文档：[代理循环如何工作](https://code.claude.com/docs/zh-CN/agent-sdk/agent-loop)（中文）；
- 逆向的系统提示词（社区成果）：[zep-us/claude-system-prompt](https://github.com/zep-us/claude-system-prompt)、[acelest/claude-code-playbook](https://github.com/acelest/claude-code-playbook)；
- 研究点：CLAUDE.md 记忆机制 / 权限模型（默认允许、plan mode、危险操作确认）/ subagents / hooks / 输出格式约定（XML 标签）——这些设计决策**在 DSH 里全都有对应物**；
- **产出**：`notes/claude-code-设计解剖.md`——"生产级 agent 设计决策清单"，作为读 DSH 前的对照表。

---

## 第 3 周：读两个开源生产级系统（逐渐变大）

### D1~D4 · T5a Aider

- `git clone --depth 1 https://github.com/Aider-AI/aider.git source/aider`。
- 学：Repo Map（仓库地图）/ SEARCH+REPLACE 精确编辑 / git 自动提交循环 / 差分编辑为什么能防"乱改"。
- 预测-对照：读前先写"如果我用 TS 写，怎么组织"，读时对照。

### D5~D7 · T5b OpenHands

- `git clone --depth 1 https://github.com/All-Hands-AI/OpenHands.git source/openhands`。
- 学：事件驱动循环 / sandbox 隔离 / 工具注册机制（与 DSH 最接近的参照系之一）。
- **产出**：`notes/aider.md`、`notes/openhands.md`（一页纸：循环/验证点/防幻觉技巧/重写想法）。

---

## 第 4 周：Cordis 框架

- D1~2：读本地 `@deepseek-ai/cordis`（`node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/cordis`），跑通 Quick Start（counter/greeter）；
- D3~4：读包内 `docs/` 教程（build-a-plugin / plugin-lifecycle / loader-config）+ [Koishi 文档·可逆插件系统](https://koishi.chat/zh-CN/cookbook/design/disposable)；
- D5~7：动手 `try/02-cordis-playground`：带配置项的插件（schemastery）+ 事件监听 + Service 注入。
- **产出**：`try/02-cordis-playground` + 一页纸 Cordis 概念图。

---

## 第 5 周：DSH 运行轨迹精读（196 包的钥匙）

**方法**：按"一次会话从启动到回答"的路径读，只看骨架。读每个包前先预测，读后只记差距。

| 天 | 读什么 | 看什么 |
|---|---|---|
| D1 | `dsh` CLI + `dsh-app-boot` + `dsh-cordis-host-runner` | profile 引导与插件加载 |
| D2 | `dsh-agent-loop` | **核心 ReAct 循环**：思考→工具→观察→再思考 |
| D3 | `dsh-tools` + 2~3 个 `dsh-tool-*` | 工具如何注册成 Cordis 插件 |
| D4 | `dsh-session` + `dsh-compaction-basic` | 上下文管理与压缩 |
| D5 | `dsh-sandbox-*` + `dsh-fs-observation-policy` + `dsh-pwsh-sandbox` | 安全壳 |
| D6 | `dsh-goal*` + `dsh-subagent*` + `dsh-workflow*` | 高层编排 |
| D7 | 汇总 | 画"DSH 运行时全景图" |

**产出**：`notes/` 每包一页纸 + 全景图。对照参考：T4 的"设计决策清单"——你会发现 DSH 的每个设计都有来处。

---

## 第 6 周：毕业项目——自研插件

- **选题（推荐）**：新工具插件 `dsh-tool-xxx`（如"读指定目录 README 并总结"）；备选：压缩策略魔改 / skill 编写 / 会话持久化。
- 流程：设计（提供什么 service/工具/配置）→ 实现 → 本地验证（`pnpm build` + `pnpm dsh` 跑 TS 入口，或 `dsh plugin` 装进 profile）→ 打磨 → 总结。
- **产出**：真实可用的插件 + 设计/踩坑记录（`notes/`）。

---

## 贯穿纪律（每天）

- 每次学习 git commit；README 学习日志表随手填；
- 一页纸笔记只记四件事：解决什么问题 / 核心机制 / 验证点在哪 / 差距点；
- 卡住超过 2 小时：去读对应 README 或换任务，不硬啃。

## 参考资源

| 类别 | 资源 |
|---|---|
| 最小 agent | [阿里云·手写最小 Agent](https://developer.aliyun.com/article/1757224)、Manning《Build an AI Agent from Scratch》 |
| 阶梯对象 | [teenycode](https://github.com/yangshun/teenycode)、[smolagents](https://huggingface.co/blog/smolagents)、[Aider](https://github.com/Aider-AI/aider)、[OpenHands](https://github.com/All-Hands-AI/OpenHands) |
| Claude Code 解剖 | [官方 agent-loop 文档（中文）](https://code.claude.com/docs/zh-CN/agent-sdk/agent-loop)、[claude-system-prompt](https://github.com/zep-us/claude-system-prompt)、[claude-code-playbook](https://github.com/acelest/claude-code-playbook) |
| 框架谱系 | [framework-analysis](https://github.com/larsderidder/framework-analysis) |
| Cordis | 本地 `@deepseek-ai/cordis` + 包内 docs/ + [Koishi 文档](https://koishi.chat/zh-CN/cookbook/design/disposable) |
| DSH | 本地 node_modules 全部源码 + [deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) + [dsh-agent-teams 插件开发指南](https://github.com/NanmiCoder/dsh-agent-teams/blob/main/docs/developing-dsh-plugins.md) |
| 可选扩展 | [AI-Agents-Automation-Roadmap](https://github.com/Hamed233/AI-Agents-Automation-Roadmap)、[ai-engineer-roadmap](https://github.com/v9ai/ai-engineer-roadmap)（读代码为主，路线图类只看不跟） |
